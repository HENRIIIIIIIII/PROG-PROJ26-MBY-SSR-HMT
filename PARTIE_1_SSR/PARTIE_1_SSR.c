#include <stdio.h>
#include <stdbool.h>

bool fichier_present = true;

int main()
{
    if (!fichier_present)
    {
        // Saisie du nom du loto + et plage des valeurs
        Saisie_new_loto();
    }
    else
    {
        // Saisie nom du fichier à ouvrir
    }

}

// 
void Saisie_new_loto()
{
    bool numCompl = true;
    bool simMode = true;

    // Saisie du nom du loto + et plage des valeurs


    // Numéros complémentaires ?
    if (numCompl) {
        // Saisie plage de valeurs
    }
    else
    {
        if (simMode)
        {
            // Saisie du nombre de valeurs aléatoires
        }
        else
        {
            // Creation du fichier
            /* goto -> fichier texte existant ? */
        }
    }

}